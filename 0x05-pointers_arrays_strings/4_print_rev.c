#incleude "main.h"
#include <string.h>
#include <stdi0.h>
/**
*print_rev - print in reverse oder
*@s:char type
**/
void print_rev(char *s)
{
	int l = strlen(str);
	int x;

	for (x = l ; x >= 0 ; x--)
	_putchar (str[x]);
}
